(deflisten net "scripts/network_info.sh")

(defwidget network_info []
  (box
   :class "network_info"
   :tooltip {net.name}
   :style "color: ${net.color};"
   {net.icon}))

(defwidget sys []
  (network_info))

(defwindow sys_win
  :monitor 0
  "TODO")
